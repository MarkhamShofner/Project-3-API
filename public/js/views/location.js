var LocationView = function(location){
  // set a Location model as a property of the view
  this.location = location;
  // sets the div with class location as the domain of this view
  this.$el = $(".location");
}

LocationView.prototype = {
  // DOM manipulation using a Location
  locationTemplate: function(location){
    var html = $("<div></div>");
    html.append("<h2>" + location.name + "</h2>");
    html.append("<p class='description'>" + location.description + "</p>")
    html.append("<img class='business-photo' alt='Photograph of " + locaiton.name + "' src='" + location.image_url + "'>");
    return(html);
  },
  // function that changes the HTML of the $el(div with class location) to what was generated by the template
  render: function(){
    var self = this;
    self.$el.html(self.locationTemplate(self.location).html());
  },
  // empties all elements within the $el (div with class location)
  clearContainer: function(){
    this.$el.empty()
  }
}
